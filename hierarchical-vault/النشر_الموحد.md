# 🚀 النشر الموحد - كل شيء في مكان واحد

## ✅ تم توحيد المشروع

الآن كل شيء في الجذر:
- ✅ Frontend (React + Vite)
- ✅ Backend (Node.js + Express)
- ✅ قاعدة البيانات (PostgreSQL)
- ✅ ملف واحد للنشر

## 🚀 النشر على Render

### الطريقة 1: Blueprint (الأسهل)
1. اذهب إلى [render.com](https://render.com)
2. اضغط "New +" → "Blueprint"
3. اختر المستودع
4. استخدم ملف `render-unified.yaml`
5. حدث متغيرات البيئة بمعلومات Neon

### الطريقة 2: Manual Deploy
1. اذهب إلى [render.com](https://render.com)
2. اضغط "New +" → "Web Service"
3. اختر المستودع
4. املأ الإعدادات:

**Basic Settings:**
- **Name**: `hierarchical-vault`
- **Environment**: `Node`
- **Root Directory**: `(فارغ - الجذر)`
- **Build Command**: `npm run install:all && npm run build`
- **Start Command**: `npm start`

**Environment Variables:**
```
NODE_ENV=production
PORT=10000
DB_HOST=YOUR_NEON_HOST_HERE
DB_PORT=5432
DB_NAME=YOUR_NEON_DATABASE_HERE
DB_USER=YOUR_NEON_USERNAME_HERE
DB_PASSWORD=YOUR_NEON_PASSWORD_HERE
DB_SSL=true
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=24h
CORS_ORIGIN=https://hierarchical-vault.onrender.com
```

## 🔧 إعداد قاعدة البيانات

### 1. إنشاء حساب Neon
1. اذهب إلى [neon.tech](https://neon.tech)
2. أنشئ حساب جديد
3. أنشئ مشروع جديد
4. احصل على معلومات الاتصال

### 2. تشغيل Schema
1. اذهب إلى Neon Console → SQL Editor
2. انسخ محتوى `database/schema-neon.sql`
3. الصق الكود واضغط "Run"

## 📝 خطوات النشر

### 1. رفع المشروع
```bash
git add .
git commit -m "Unified project structure"
git push origin main
```

### 2. النشر على Render
1. اتبع الطريقة 1 أو 2 أعلاه
2. انتظر اكتمال البناء
3. احصل على URL

### 3. اختبار المشروع
- اذهب إلى URL
- يجب أن ترى صفحة تسجيل الدخول
- جرب إنشاء حساب جديد

## 🎯 المميزات

- ✅ **خدمة واحدة** - Backend + Frontend
- ✅ **URL واحد** - لا حاجة لـ CORS
- ✅ **أسهل في الإدارة** - كل شيء في مكان واحد
- ✅ **أرخص** - خدمة واحدة بدلاً من اثنتين

## 🔧 استكشاف الأخطاء

### مشاكل شائعة:
1. **خطأ في البناء**: تحقق من logs
2. **خطأ في قاعدة البيانات**: تأكد من معلومات Neon
3. **خطأ في الصفحات**: تحقق من مسارات الملفات

### فحص Logs:
1. اذهب إلى Service في Render
2. اضغط "Logs"
3. ابحث عن الأخطاء

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من متغيرات البيئة
2. راجع logs في Render
3. تأكد من صحة معلومات Neon
4. تحقق من مسارات الملفات

---
**الآن جرب النشر الموحد!** 🚀